# MCP Demo

Set `NEXUS_MCP_ENABLED=true` before starting the daemon. The MVP bridge exposes local-only tool and resource names in code and documentation; MCP host wiring depends on your host configuration.

```bash
python main.py
```

